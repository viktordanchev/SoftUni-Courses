function sumFirstAndLastElements(elements) {
  const firstElement = elements[0];
  const lastElement = elements[elements.length - 1];
  console.log(firstElement + lastElement);
}

sumFirstAndLastElements([20, 30, 40]);
