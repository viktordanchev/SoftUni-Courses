function findBiggestNum(firstNum, secondNum, thirdNum) {
  const largestNumber = Math.max(firstNum, secondNum, thirdNum);
  console.log(`The largest number is ${largestNumber}.`);
}

findBiggestNum(5, -3, 16);